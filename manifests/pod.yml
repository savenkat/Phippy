apiVersion: v1
kind: Pod
metadata:
  name: phippy-pod
  labels:
    app: phippy
spec:
  containers:
  - name: phippy
    image: shashankbarsin/phippy
    imagePullPolicy: Always
    ports:
    - containerPort: 8000