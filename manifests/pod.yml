apiVersion: v1
kind: Pod
metadata:
  name: phippy
  labels:
    app: phippy
spec:
  containers:
  - name: phippy
    image: shashankbarsin/phippy
    imagePullPolicy: Always
    ports:
    - containerPort: 8000